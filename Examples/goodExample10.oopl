void one();

int largest(int x, int y);

int largest(int x, int y)[]
{
	if (x > y) {
		return x;
	} else {
		return y;
	}
}

int largest;

void one()[]
{
	print("One", 1);
}

int main()
[
	int two;
]
{
	largest = 100;
	one();
	read(two);
	largest(1, two);
	print(largest, two);
	return 1;
}